services:
  - type: web
    name: mission-control
    env: static
    buildCommand: echo "No build needed - static HTML"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
